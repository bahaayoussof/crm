{
  "name": "customer-support-crm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "lint": "npm --prefix client run lint && npm --prefix server run lint",
    "typecheck": "npm --prefix client run typecheck && npm --prefix server run typecheck",
    "test": "npm --prefix client run test && npm --prefix server run test",
    "seed:test": "npm --prefix server run seed:test",
    "db:seed:test": "npm --prefix server run db:seed:test",
    "db:demo:deploy": "npm --prefix server run db:demo:deploy",
    "demo:seed": "npm --prefix server run demo:seed",
    "demo:reset": "npm --prefix server run demo:reset"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
